[package]
name = "minigrep"
license = "MIT OR Apache-2.0"
description = "Yet another implement of `grep` written in Rust."
version = "0.1.0"
edition = "2021"

[profile.dev]
opt-level = 1

[profile.release]
opt-level = 3

[dependencies]
